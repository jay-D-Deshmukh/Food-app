import createSlice from "@reduxjs/toolkit";

const initialState = {
    item:[]
};

export const createSlice = createSlice({
    name:cart,
    initialState,
    

    reducers: {
        addItem:(satate, action)=>{
            
        }
    }
})
